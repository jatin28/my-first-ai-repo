from __future__ import annotations

from agent import SimpleRAGAgent

agent = SimpleRAGAgent()

# This is a placeholder for a Hugging Face Spaces deployment.
# For a real deployment, swap this for a Gradio or Streamlit UI.
# Example:
# import gradio as gr
#
# def chat(question, history):
#     return agent.ask(question, history)
#
# gr.ChatInterface(chat).launch()

print("HF Spaces app scaffold ready. Add a UI framework to use it.")
